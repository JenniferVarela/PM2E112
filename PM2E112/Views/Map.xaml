<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:map="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="PM2E112.Views.Map">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding latitud}" x:Name="mtxtLat" IsVisible="false"/>
            <Entry Text="{Binding longitud}" x:Name="mtxtLon" IsVisible="false"/>
            <Label Text ="Sitio" FontSize="16"/>
            <Label Text="{Binding descripcion}" FontSize="16" HorizontalOptions="Center"/>
            <map:Map x:Name="Mapa"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>